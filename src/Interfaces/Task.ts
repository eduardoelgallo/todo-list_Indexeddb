

interface Task {
    title: string | string,
    hour: number| string,
    minutes: number| string,
    day: number | string,
    month: number | string,
    year: number | string,
    notified: string
  }

export default Task;